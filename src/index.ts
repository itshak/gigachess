// PureChess workstation — baseline stub (Phase 1)
// Phase 1 does not migrate from chessops to purechess; this file keeps `npm run typecheck` green.
// Purechess library implementation lands in Phase 2+ (separate change).
export const PURECHESS_BASELINE = "purechess-library baseline stub" as const;
export type BaselineTag = typeof PURECHESS_BASELINE;
