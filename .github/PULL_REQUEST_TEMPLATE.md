## 📝 Description

<!-- Briefly describe the purpose of this PR and what problem it solves. -->

## ⚡ Performance Impact

<!-- If this change touches hot paths (movegen, bitboards, FEN/PGN parser), include benchmark results from `npm run bench:real -- --quick` -->

## ✅ Checklist

- [ ] `npm run typecheck` passes with zero errors.
- [ ] `npm test` passes (all 165+ tests green).
- [ ] `npm run bench:real -- --quick` passes (all 24 benchmark gates met).
- [ ] Added unit tests covering new features / bug fixes.
- [ ] Preserves 100% exact parity with `chess.js` & `chessops` specifications.
