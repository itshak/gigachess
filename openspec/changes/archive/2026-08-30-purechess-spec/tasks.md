## 1. Change scaffolding and clean-room verification

- [ ] 1.1 Verify `openspec status --change purechess-spec --json` shows proposal:done, specs:ready, design:ready and `refs/README.md` wall intact, verify `git check-ignore refs/gpl-only/chessops` shows gitignored and `rg -n "GPL" src/` empty — verification: `openspec status --change purechess-spec --json | jq .artifacts` and `cat .gitignore | grep refs/gpl-only` and `rg -n GPL src/ || true` produce expected outputs
- [ ] 1.2 Confirm `bench/README.md` pinning and `bench/magic-tables/*.json` placeholder exists before overwrite, verify `shasum -a 256 bench/data/lichess_db.sample.pgn` matches `f5c064...` — verification: `ls bench/magic-tables/` shows rook.json bishop.json, `shasum -a 256 bench/data/lichess_db.sample.pgn` prints pinned hash

## 2. purechess-rules spec (FIDE 2023 + Chess960)

- [ ] 2.1 Write `specs/purechess-rules/spec.md` with `## Purpose` 50+ chars covering FIDE 2023 + Chess960 X-FEN/Shredder, and ADDED Requirements for FEN six-field validation (Kings=2, pawns not on backrank, oppositeCheck, halfmove/fullmove, enPassant rank) with SHALL/MUST and `#### Scenario:` WHEN/THEN — verification: `openspec validate --changes --json` shows no `Purpose too brief` and file exists with `rg "### Requirement:" specs/purechess-rules/spec.md` count ≥6
- [ ] 2.2 Add castling truth-table requirement (standard E1→G1/C1 and 960 Gg/HAha, king ends g1/c1, rook ends f1/d1, through-check, empty path, X-FEN vs Shredder dual input/X-FEN output) with Given/When/Then scenarios — verification: `rg -n "castling" specs/purechess-rules/spec.md` shows table and `rg "#### Scenario:"` count ≥4 for that requirement
- [ ] 2.3 Add en passant / promotion / double push requirement (immediate en passant only, =Q/R/B/N, 0-0 tolerance) with WHEN/THEN — verification: `rg "enPassant|promotion" specs/purechess-rules/spec.md` present and `openspec validate --changes --strict --json` passes for that file
- [ ] 2.4 Add check/checkmate/stalemate/insufficient material and 50-move/75-move/threefold/fivefold + SAN/UCI semantics (disambiguation, +#, =Q, O-O, e7e8q, e1g1 vs e1h1) with byte-identical `+`/`#` vs chessops — verification: `rg "isCheckmate|isStalemate|isInsufficientMaterial|parseSan|parseUci" specs/purechess-rules/spec.md` shows all, and `rg "purechess\.fen\.|purechess\.san\."` shows i18n keys `en/ru/he` mentioned
- [ ] 2.5 Add Chess960 X-FEN/Shredder round-trip and i18n/a11y requirement (keys `purechess.fen.*` in en/ru/he, keyboard `[ ]` Alt+ chords, AriaLiveAnnouncer, `enableArrowMoveShortcuts` OFF) — verification: `rg "i18n|ru|he|AriaLiveAnnouncer|Alt\+" specs/purechess-rules/spec.md` present and `rg "#### Scenario:"` exists under that requirement

## 3. purechess-board-movegen spec (Board {lo,hi} + Black Magic)

- [ ] 3.1 Write `specs/purechess-board-movegen/spec.md` with `## Purpose` 50+ chars (board encoding, {lo,hi} pair wins, Black Magic 441% vs HQ, forbid BigInt per ADR-012) and ADDED Requirement for SquareSet {lo,hi} ops (and/or/xor/not/minus/shl/shr/popcnt/first) pure, no BigInt in src/squareSet.ts hot path — verification: file exists, `rg "SquareSet" specs/purechess-board-movegen/spec.md` and `rg "BigInt" specs/purechess-board-movegen/spec.md` shows forbid, `openspec validate --changes --json` passes
- [ ] 3.2 Add Board value requirement ({white,black,pawn,knight,bishop,rook,queen,king,occupied,promoted} readonly, occupied invariant, promoted, clone→mutate clone) with WHEN/THEN — verification: `rg "type Board" specs/purechess-board-movegen/spec.md` and `rg "#### Scenario:"` under it
- [ ] 3.3 Add leaper attacks requirement (knight/king/pawn tables 64 entries) with table-driven pure semantics — verification: `rg "knightAttacks|kingAttacks|pawnAttacks" specs/purechess-board-movegen/spec.md` present
- [ ] 3.4 Add Black Magic sliding requirement per-square mask/magic/shift/offset + flat attackTable (rook 102400 bishop 5248, Fancy per-square shift=64-popcount, offset cumulative, index=((occ&mask)*magic>>shift)+offset, Math.imul lo/hi emulation, sideEffects:false, forbids BigInt) reference ADR-012 plain fixed-shift wins 441% and uniform 8192 stub → per-square size opt, note bench/magic-tables/*.json via MIT RecklessMagics/magic-bits not GPL — verification: `rg "Black Magic|magic.*shift.*offset|RecklessMagics|102400|5248" specs/purechess-board-movegen/spec.md` and `rg "BigInt" specs/purechess-board-movegen/spec.md` shows forbid, and `cat bench/magic-tables/rook.json | jq .meta.tableSize` is 102400
- [ ] 3.5 Add ray/between/isAttacked/kingAttackers and dests/isLegal/allDests parity vs chessops (pins, en passant discovered check, 1k random pos byte-identical) + bundle tree-shaking gate (purechess/core ≥30% smaller, sideEffects:false, exports map) — verification: `rg "between|ray|kingAttackers|dests|isLegal" specs/purechess-board-movegen/spec.md` and `rg "purechess/core"` mentions bundle
- [ ] 3.6 Add i18n/a11y for movegen errors (purechess.move.* keys, AriaLive, [ ] Alt+ chords, enableArrowMoveShortcuts OFF) — verification: `rg "purechess\.move|Alt\+" specs/purechess-board-movegen/spec.md` and scenario with WHEN/THEN

## 4. purechess-pgn-fen spec (PGN ABNF + streaming + FEN/SAN/UCI + perft)

- [ ] 4.1 Write `specs/purechess-pgn-fen/spec.md` with `## Purpose` 50+ chars (streaming PGN, FEN, SAN, UCI, GameTree re-specified from pgn-chess-tree behavior not source, perft oracle) and PGN ABNF requirement (Headers, *,1-0, {} comments, ; line, () variations, $NAG, SAN disambiguation, Seven Tag Roster, % escape) — verification: file exists, `rg "ABNF|Header.*Result" specs/purechess-pgn-fen/spec.md` and `rg "#### Scenario:"` count ≥3
- [ ] 4.2 Add streaming chunk parser state machine requirement (feed(chunk) + finish, states HeaderKey/HeaderValue/Movetext/CommentBrace/VariationDepth/StringEscape, stack depth, GameTree shape {headers:Map, moves:[{san,nags,comments,variations}]}) re-specified from pgn-chess-tree behavior, never copy source, games/s ≥50% vs chessops heap ≤110% — verification: `rg "feed\(chunk|GameTree" specs/purechess-pgn-fen/spec.md` and state table present
- [ ] 4.3 Add parseFen/makeFen requirement (six fields, Shredder vs X-FEN tolerant input X-FEN output, Result<FenError> i18n, 10k FEN throughput ≥20% vs chessops byte-identical) — verification: `rg "parseFen|makeFen" specs/purechess-pgn-fen/spec.md` and `rg "purechess\.fen"` shows keys
- [ ] 4.4 Add parseSan/makeSan + parseUci/makeUci requirement (SAN disambiguation minimal, x,+ #, =Q, O-O tolerance 0-0, UCI e7e8q lower-case, e1g1/e1h1 castling, Result<SanError> i18n, SAN byte-identical vs chessops for AriaLiveAnnouncer) — verification: `rg "parseSan|makeSan|parseUci|makeUci" specs/purechess-pgn-fen/spec.md` and SAN table present
- [ ] 4.5 Add Result error handling requirement (Result<T,E> ok true/false never throw, codes fen/san/uci/pgn kebab-case mapping to purechess.*.<code> en/ru/he) and perft oracle requirement (perft(6)=119060324, nodes/s ≥parity vs chessops, 0:1,1:20,2:400,3:8902,4:197281,5:4865609) — verification: `rg "Result<|perft.*119060324" specs/purechess-pgn-fen/spec.md` and scenarios with WHEN perft(6) THEN 119060324
- [ ] 4.6 Add i18n/a11y for PGN flow (purechess.pgn.* keys en/ru/he, [ ] Home End Alt+ chords, enableArrowMoveShortcuts OFF, AriaLive queue-safe) — verification: `rg "purechess\.pgn|enableArrowMoveShortcuts|AriaLive" specs/purechess-pgn-fen/spec.md`

## 5. Bench magic tables (MIT, per-square, not GPL)

- [ ] 5.1 Generate bench/magic-tables/rook.json and bishop.json via MIT RecklessMagics (codedeliveryservice/RecklessMagics, seed 0xFFAAB58C5833FE89) with per-square mask/magic/shift/offset + flat attackTable (rook 102400 bishop 5248, shift 64-popcount, Fancy) and include maskLo/maskHi, magicHex/magicLo/magicHi, shift, offset, size, table (legacy Uint32 low bits for harness) + attackTable (full {lo,hi} for impl), meta license MIT, not copied from chessops/Stockfish — verification: `ls -lh bench/magic-tables/*.json` exists, `jq .meta.license bench/magic-tables/rook.json` is MIT, `jq .magics[0].shift bench/magic-tables/rook.json` varies per-square (not uniform 11), `jq .meta.tableSize bench/magic-tables/bishop.json` is 5248, `rg GPL bench/magic-tables/` empty, and `node bench/bench-sliding.mjs --algo black-magic --iters 200000` still ≥40 MQueens/s (441% gate passes)
- [ ] 5.2 Verify uniform shift 11 baseline replaced with per-square size opt and JSON checked in, run `shasum -a 256 bench/magic-tables/*.json` recorded in spec note — verification: `cat bench/magic-tables/rook.json | jq .meta.shiftType` mentions per-square fancy and `jq .magics | map(.shift) | unique | length` >1

## 6. Design and validation

- [ ] 6.1 Write design.md covering JS-only v1, TS functional pure API (clone→mutate clone) + imperative hot-loop allowlist (src/attacks/**, src/squareSet/** only), Board {lo,hi} no BigInt, Black Magic per-square Fancy vs HQ, module split purechess/core + purechess/pgn + purechess/chess960 sideEffects:false exports map, TS ES2020/ESNext const enum/downlevelIteration:false, a11y (AriaLive, [ ] Alt+), no new runtime deps, referencing ADR-001/010/012 — verification: `cat design.md | grep -E "JS-only|clone→mutate|purechess/core|ES2020|const enum|RecklessMagics|AriaLive"` present and `openspec validate --changes --json` passes
- [ ] 6.2 Re-run `openspec status --change purechess-spec --json` and `openspec validate --changes --json` (and --strict) after each artifact, confirm specs:ready design:ready tasks:pending→done, and file layout `ls openspec/changes/purechess-spec/specs/purechess-*/spec.md` shows 3 files — verification: `openspec status --change purechess-spec --json | jq .artifacts` shows specs done design done tasks done, `openspec validate --changes --strict --json | jq .valid` is true
- [ ] 6.3 Final verification: run `npm run typecheck` (TS strict, target ES2020, no src/ impl yet so should pass) and `cargo check` in src-tauri (no Rust change) and flag VoiceOver/NVDA a11y testing required for next impl change (keyboard [ ] Alt+ chords, AriaLiveAnnouncer SAN parity, enableArrowMoveShortcuts OFF) — verification: `npm run typecheck` exits 0, `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 (or reports no errors), and tasks.md contains flag "VoiceOver/NVDA" and "[ ]" and "Alt+" keywords

